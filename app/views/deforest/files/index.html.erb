<% @dirs.each do |d| %>
  <span><%= link_to d, files_path(path: @path + "/#{d}") %></span>
  <br/>
<% end %>
<% @files.each do |f| %>
  <span><%= link_to f, file_path(path: @path, file_name: f) %></span>
  <br/>
<% end %>